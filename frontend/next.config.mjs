/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost'], // Adicione domínios se você estiver carregando imagens de fontes externas
    },
};

export default nextConfig;
